# Heater RX capture — reported OFF status — 2026-08-09

## Provenance

- Source: working Raspberry Pi / Node-RED controller
- Node: `Input Diagnostics`, directly connected to `Heater RX`
- Direction: heater to controller (RX)
- Screenshot interval: approximately 14:20:47 through 14:21:29 local time
- Operating state: the user confirmed that the heater was physically off;
  byte 14 was `0`, matching the existing `off` mapping
- Observation: 22 copied debug messages were byte-identical
- This frame was received from real hardware; it was not generated by the
  Python frame builder.

## Raw frame

Decimal:

```text
[170, 4, 19, 0, 15, 0, 1, 0, 30, 127, 0, 128, 1, 47, 0, 0, 0, 0, 0, 0, 0, 0, 0, 96, 109, 160]
```

Hexadecimal:

```text
AA 04 13 00 0F 00 01 00 1E 7F 00 80 01 2F 00 00 00 00 00 00 00 00 00 60 6D A0
```

## Confirmed properties for this status response

| Property | Observation |
|---|---|
| Total length | 26 bytes |
| Start marker | `AA` |
| Source/device | `04` (heater) |
| Payload length | `13` = 19 bytes |
| Length rule | `19 + 7 = 26` |
| Reserved byte | `00` (meaning still unknown) |
| Command | `0F` (status) |
| Received CRC | `6D A0` |
| Calculated CRC | `6D A0` |
| CRC serialization | high byte first |

Known status fields decoded by the existing Node-RED rules:

| Absolute index | Raw value | Interpretation |
|---:|---:|---|
| 11 | `80` = 128 | 12.8 V |
| 13 | `2F` = 47 | glow-plug raw value 47 |
| 14 | `00` | heater off |
| 19 | `00` | fan raw value 0 |

The remaining payload bytes are preserved but not interpreted. Repetition of
this one frame confirms a stable, user-confirmed heater-off observation, not
the meaning of unknown fields or the framing rules of every other response
command. The reported 12.8 V was not independently checked with a meter.
