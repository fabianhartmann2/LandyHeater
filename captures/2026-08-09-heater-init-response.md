# Heater RX capture — initialization response — 2026-08-09

## Provenance

- Source: working Raspberry Pi / Node-RED controller
- Node: `Input Diagnostics`, directly connected to `Heater RX`
- Direction: heater to controller (RX)
- Trigger: the existing `Init after boot` inject node on the `Planar Heater`
  tab was clicked exactly once; no deploy or Node-RED restart was used
- Safety context confirmed by the user: heater physically off, dashboard target
  off, and no timer due during the capture
- This frame was received from real hardware; it was not generated by the
  Python frame builder.

## Raw frame

Decimal:

```text
[170, 4, 5, 0, 4, 18, 138, 0, 61, 214, 203, 166]
```

Hexadecimal:

```text
AA 04 05 00 04 12 8A 00 3D D6 CB A6
```

## Confirmed properties for this initialization response

| Property | Observation |
|---|---|
| Total length | 12 bytes |
| Start marker | `AA` |
| Source/device | `04` (heater) |
| Payload length | `05` = 5 bytes |
| Length rule | `5 + 7 = 12` |
| Reserved byte | `00` (meaning still unknown) |
| Command | `04` (initialization) |
| Payload | `12 8A 00 3D D6` |
| Received CRC | `CB A6` |
| Calculated CRC | `CB A6` |
| CRC serialization | high byte first |

The five payload bytes are preserved exactly. Their meaning, stability across
heater models, and possible model/version/configuration content remain unknown.
